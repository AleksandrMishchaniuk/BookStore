%h3.text-center ORDER SUMMARY
.text-right.order_summary
  .item_total
    Item total: $
    %span.price
      = '%.02f' % @order.item_total
  - if @order.item_discount > 0
    .item_discount
      Item discount: -$
      %span.price
        = '%.02f' % @order.item_discount
  - if @step > 1 || @order.delivery
    .order_shipping
      Shipping: $
      %span.price
        = '%.02f' % (@order.delivery.try(:price) || 0)
  .order_total
    Order total: $
    %span.price
      = '%.02f' % @order.order_total
